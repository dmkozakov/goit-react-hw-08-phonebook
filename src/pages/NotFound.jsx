import { Container } from '@mui/material';

export function NotFound() {
  return (
    <Container
      maxWidth="xl"
      sx={{
        display: 'flex',
        justifyContent: 'center',
      }}
    >
      Not Found
    </Container>
  );
}
